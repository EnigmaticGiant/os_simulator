#ifndef SCHEDULER__
#define SCHEDULER__

//structs for scheduler
typedef struct instructionBlock I_Block;
typedef struct pageTable PageTable;
typedef struct process Process;

// functions needed to run the schdeuler added here

#endif